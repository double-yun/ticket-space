# Passkey 기반 암표 거래 방지 시스템

---

## 🎯 목적  
생체 인증과 비대칭키 암호화를 활용하여, **계정 양도나 판매를 통한 암표 거래를 원천 차단**.

---

## 🔑 핵심 원리  
- **개인키**는 기기의 **HSM(Hardware Security Module)** 안에 안전하게 저장되어 외부로 추출 불가.  
- **공개키**는 서버 DB에 저장.  
- **로그인 시 개인키 서명 검증**을 통해 인증.  
- 동일 계정이라도 **다른 기기에서는 로그인 불가능**.

---

## ⚙️ 기술 개요  
| 항목 | 내용 |
|------|------|
| 키 저장소 | Android Keystore, iOS Keychain |
| 암호화 알고리즘 | ECDSA P-256 |
| 서명 과정 | SHA-256 해시 + ECDSA 서명 |
| 개인키 사용 조건 | 생체 인증 완료 시에만 개인키 사용 가능 |

---

## 🧭 주요 동작 흐름  

### 1️⃣ 계정 생성  
A. 계정 생성 시 클라이언트가 **기기 HSM 내에서 키페어 생성**  
B. **개인키**는 추출 불가, **공개키**는 서버로 전송 및 DB 저장  
C. **복구 코드** 1회 생성 및 사용자에게 표시  

---

### 2️⃣ 로그인 인증  
A. 서버가 **일회용 Challenge** 발급  
B. 클라이언트가 **생체 인증 후 개인키로 Challenge 서명**  
C. 서버가 **공개키로 서명 검증**, 성공 시 **JWT 토큰 발급**  

---

### 3️⃣ 티켓 구매  
A. 서버가 사용자의 **공개키를 AES-256으로 암호화**  
B. 암호화된 키를 **블록체인 SBT에 영구 기록**  
C. 티켓은 **구매 당시의 공개키에 귀속된 형태로 존재**

---

### 4️⃣ 티켓 검증  
A. 티켓 사용 시 **QR 코드 생성**  
B. 서버가 **계정의 공개키와 티켓 내 암호화된 공개키 복호화 후 비교**  
C. 키가 다르면 **계정 복구나 양도 발생으로 판단**, **티켓 사용 차단**

---

### 5️⃣ 계정 복구  
A. 기기 분실/변경 시 **복구 코드 입력으로 계정 복구 가능**  
B. 복구 시 **새 키페어 생성 및 공개키 DB 갱신**  
C. 공개키가 변경되므로 **기존 티켓은 자동 무효화**

---

## 🚫 암표 거래 방지 시나리오  

### 1️⃣ 계정 판매  
- 다른 사람이 계정을 구매해도 개인키는 기기 내부에만 존재  
- **새 기기에서는 서명 검증 불가 → 로그인 불가능**

### 2️⃣ 복구 코드 판매  
- 복구 시 **새 공개키가 생성**,  
- 기존 블록체인 티켓에 기록된 공개키와 불일치  
- **모든 기존 티켓 자동 무효화**

### 3️⃣ SBT 전송 시도  
- **EIP-5192 표준**을 따른 **스마트 컨트랙트**가  
  **전송을 강제 차단 → 토큰 이동 불가**

---

